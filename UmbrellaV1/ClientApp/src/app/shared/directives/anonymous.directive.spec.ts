import { AnonymousDirective } from './anonymous.directive';

describe('AnonymousDirective', () => {
  it('should create an instance', () => {
    const directive = new AnonymousDirective();
    expect(directive).toBeTruthy();
  });
});
