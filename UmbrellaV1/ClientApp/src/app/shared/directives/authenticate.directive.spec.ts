import { AuthenticateDirective } from './authenticate.directive';

describe('AuthenticateDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthenticateDirective();
    expect(directive).toBeTruthy();
  });
});
